import { AccountDialog } from "../components/User/AccountDialog";

type Props = {};
const LoginPage = ({}: Props) => {
  const isOpen = true;
  const onClose = () => {};

  return;
};

export default LoginPage;
